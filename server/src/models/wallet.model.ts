// This file is no longer used for token caching
// If needed elsewhere, retain only the required functionality

// If this file is imported elsewhere but the functions aren't used,
// you can keep empty function signatures that return default values
export async function saveWalletData() {
  console.log('Token caching has been disabled');
  return null;
}

export async function getWalletByAddress() {
  console.log('Token caching has been disabled');
  return null;
}